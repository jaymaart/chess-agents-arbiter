{
  "name": "chess-arbiter",
  "version": "1.0.0",
  "description": "Community arbiter node for Chess Agents — fetches, verifies, and executes signed match jobs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "dotenv": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
